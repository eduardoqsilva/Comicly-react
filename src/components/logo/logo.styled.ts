import styled from "styled-components";

export const LogoStyled = styled.div`
  position: absolute;
  left: 1rem;
  top: 1rem;
  z-index: 2;

  & img {
    width: 60px;
    height: 60px;
  }
`